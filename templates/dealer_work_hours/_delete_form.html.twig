<form method="post" action="{{ path('app_dealer_work_hours_delete', {'id': dealer_work_hour.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dealer_work_hour.id) }}">
    <button class="btn">Delete</button>
</form>
